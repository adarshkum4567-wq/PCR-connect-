name: gps_saver_sos
description: A Flutter based SOS Emergency Application.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Hum 'any' use karenge taaki Codemagic bina error ke build kare
  geolocator: any
  firebase_core: any
  firebase_database: any
  permission_handler: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
